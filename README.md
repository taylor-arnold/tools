

```
  # Install dependencies
  uv sync --dev

  # Run the CLI
  uv run ptools --help
  uv run ptools hello
  uv run ptools hello Alice

  # Run tests
  uv run pytest

  # Lint code
  uv run ruff check

```